import { NumberCharDirective } from './number-char.directive';

describe('NumberCharDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberCharDirective();
    expect(directive).toBeTruthy();
  });
});
